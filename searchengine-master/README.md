# Search Engine - Локальный поисковый движок

## 📋 Описание проекта

Поисковый движок для индексации и поиска по веб-сайтам с поддержкой морфологии русского языка.

Проект представляет собой многопоточный поисковый движок для индексации сайтов и выполнения поисковых запросов. Реализован на Java и Spring Framework. Включает парсинг страниц, извлечение контента, лемматизацию, расчёт релевантности и предоставление API для управления индексированием и поиска.

## Основные возможности
- Параллельная индексация сайтов и страниц через ForkJoinPool.
- Извлечение текста и вычисление лемм.
- Подсчёт частоты лемм и формирование поисковых индексов.
- Поиск по ключевым словам с ранжированием результатов.
- REST API для запуска/остановки индексации и выполнения поиска.
- Статистика по страницам, леммам, статусам сайтов.
- Кэширование для исключения дублирования индексации страниц при многопоточном взаимодействии с базой данных.

## 🔧 Технологии
- Backend: `Java 17, Spring Boot, Spring Data JPA`
- База данных: `MySQL 8.0`
- Парсинг: `Jsoup`
- Лемматизация: `Lucene Morphology`
- Многопоточность: `ForkJoinPool`

## 📡 API Эндпоинты

### Индексация
- `GET /api/startIndexing` - запуск индексации
- `GET /api/stopIndexing` - остановка индексации
- `POST /api/indexPage` - индексация отдельной страницы

### Поиск
- `GET /api/search` — поиск по запросу и/или сайту

### Статистика
- `GET /api/statistics` — сводная статистика по проекту

## Установка и запуск

### Требования
- Java 17+
- Maven 3.x
- MySQL

### Загрузка проекта
Для загрузки проекта с GITHUB: 
```bash
git clone https://github.com/archibaldas/SearchEngene.git
cd SearchEngene
mvn clean install
```

### Настройка Базы Данных
Программа настроена на работу с реляционной СУБД MySQL, убедитесь, что она установлена на вашем устройстве. База данных создается автоматически и запускается при условии использования данных пользователя с доступом к редактированию.

Так же можно создать в MySQL базу самостоятельно командой:

```amplicode sql
CREATE DATABASE IF NOT EXISTS search_engine_db 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;
```

Название базы можно использовать свое, при этом необходимо внести соответствующие изменения в настройках `application.yml`

Таблицы подготавливаются с помощью технологии Hibernate.

Если MYSQL используется впервые, необходимо подключится к ней с `root` пользователем и выполнить команды:
```amplicode sql
CREATE USER IF NOT EXISTS '{имя_пользователя}'@'localhost' IDENTIFIED BY 'пароль_пользователя';
GRANT ALL PRIVILEGES ON search_engine_db.* TO '{имя_пользователя}'@'localhost';
FLUSH PRIVILEGES;
```
Перед запуском укажите параметры подключения к базе в `application.yml`.

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/search_engine_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: {Имя пользователя}
    password: {Пароль пользователя}
    driver-class-name: com.mysql.cj.jdbc.Driver
```

## Настройки извлечения лемм

Для извлечения лемм при индексации страниц используется библиотека из репозитория `skillbox-gitlab`.
* Все необходимые зависимости добавлены в `pom.xml`, но возможно для вашего устройства будет необходимо получить токен для доступа к репозиторию.

Проверьте файл settings.xml

* В Windows он располагается в директории
`C:/Users/<Имя вашего пользователя>/.m2`
* В Linux — в директории
`/home/<Имя вашего пользователя>/.m2`
* В macOs — по адресу
`/Users/<Имя вашего пользователя>/.m2`

Если файла settings.xml нет, создайте его и вставьте в него код:

```xml
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
https://maven.apache.org/xsd/settings-1.0.0.xsd">
    <servers>
        <server>
            <id>skillbox-gitlab</id>
            <configuration>
                <httpHeaders>
                    <property>
                        <name>Private-Token</name>
                        <value>ВАШ_ТОКЕН_ДОСТУПА</value>
                    </property>
                </httpHeaders>
            </configuration>
        </server>
    </servers>
</settings>
```

В блоке `<value>` находится уникальный токен доступа. Скопируйте из документа по ссылке: [Актуальный токен](https://docs.google.com/document/d/1rb0ysFBLQltgLTvmh-ebaZfJSI7VwlFlEYT9V5_aPjc/edit?tab=t.0)  
И вставьте вместо `ВАШ_ТОКЕН_ДОСТУПА`


Обязательно почистите кэш maven. Самый надёжный способ — удалить
директорию:
* Windows: C:\Users\<user_name>\.m2\repository
* macOs: /Users/<user_name>/.m2/repository
* Linux: /home/<user_name>/.m2/repository  

<user_name> — имя пользователя, под которым вы работаете.
После этого снова попробуйте обновить данный из pom.xml.

### 🏃‍♂️ Запуск приложения

#### Запуск через Maven

Сначала перейдите в директорию проекта:
```
cd {Каталог проекта}~/SearchEngene/searchengine-master
```


``` bash
# Сборка проекта
mvn clean install

# Запуск приложения
mvn spring-boot:run
```

#### 🌐 Проверка работоспособности
После успешного запуска программы введите в окне браузера:
`http://localhost:8080`  
`8080` - порт установленный по умолчанию. При необходимости его можно изменить в настройках `application.yml`

```yaml
server:
  port: 8080
```

На главной странице должен открыться веб-интерфейс со статистикой индексации `DASHBOARD`.  
Во вкладке `MANAGEMENT`:
 - `StartIndexing` - запуск полной индексации, кнопка сменяется на `StopIndexing`
 - `StopIndexing` - остановка индексации, кнопка сменяется на `StartIndexing`
 - Поле ввода для индексации отдельной ссылки, `ADD\UPDATE`  

Во вкладке `SEARCH`
 - Поле выбора сайта для поискового запроса, или же по всем сайтам
 - Поле ввода поискового запроса, и кнопка `SEARCH`

#### Список индексируемых сайтов

Можно изменить в конфигурации `application.yml`

```yaml
indexing-settings:
  sites:
    - url: http://www.playback.ru
      name: PlayBack
    - url: https://ipfran.ru
      name: ИПФ РАН
    - url: https://nikoartgallery.com/
      name: NikoArt
    - url: https://www.svetlovka.ru
      name: Svetlovka
    - url: https://www.lutherancathedral.ru/
      name: LutheranCathedral
```

## Примечания
Проект разработан в учебных целях. Архитектура позволяет расширять функциональность: добавлять новые сайты, усложнять алгоритмы ранжирования, улучшать парсер, подключать дополнительные аналитические инструменты.
